{
    "name": "Refresh token",
    "request": {
        "method": "PUT",
        "path": "/v1/login",
        "body": {
            "token": "${authToken}",
            "refreshToken": "${refreshToken}"
        },
        "useToken": false
    },
    "response": {
        "status": 200
    }
}